# For act: use KEY=value (no space). Load with: act --secret-file .secrets
# When running act in Docker, use host.docker.internal so the runner can reach ClickHouse on the host.
CLICKHOUSE_HOST=host.docker.internal
CLICKHOUSE_PORT=9000
CLICKHOUSE_USER=exporter
CLICKHOUSE_PASSWORD=test
